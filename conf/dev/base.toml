# This is base config

[base]
    debug_mode="debug"
    time_location="Asia/Chongqing"

[log]
    log_level = "trace"         #日志打印最低级别
    [log.file_writer]           #文件写入配置
        on = true
        log_path = "./go_evnconfig.inf.log"
        rotate_log_path = "./go_evnconfig.inf.log"
        wf_log_path = "./go_evnconfig.wf.log"
        rotate_wf_log_path = "./go_evnconfig.wf.log"
    [log.console_writer]        #工作台输出
        on = true
        color = true